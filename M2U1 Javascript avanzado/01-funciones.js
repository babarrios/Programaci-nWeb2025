function sumar(a, b) {
    return a + b;
}

const resultado = suma(3, 5);
console.log("El resultado de la suma es: " + resultado); //acordate que el texto va entre comillas y se le suma la variable resultado

